"use client";
import { ArrowRight,Check,LoaderCircle } from "lucide-react";
import { FormEvent,useState } from "react";
export function ActivationForm({publicCode}:{publicCode:string}){const[loading,setLoading]=useState(false);const[error,setError]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setLoading(true);setError("");const form=new FormData(event.currentTarget);try{const response=await fetch("/api/activate",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({publicCode,label:form.get("label"),destinationType:form.get("destinationType"),destinationUrl:form.get("destinationUrl")})});const data=await response.json()as{error?:string;redirectUrl?:string};if(!response.ok)throw new Error(data.error||"Activation failed");location.assign(data.redirectUrl||"/dashboard")}catch(value){setError(value instanceof Error?value.message:"Activation failed");setLoading(false)}}return <form className="activation-form" onSubmit={submit}><div className="code-confirmation"><span>YOU’RE ACTIVATING</span><strong>{publicCode}</strong></div><label>Product name<input name="label" placeholder="Clean Kicks front counter" required/></label><label>Where should it go?<select name="destinationType"><option value="google">Google review page</option><option value="facebook">Facebook page</option><option value="instagram">Instagram profile</option><option value="linkedin">LinkedIn page</option><option value="whatsapp">WhatsApp</option><option value="tiktok">TikTok profile</option><option value="website">Website</option><option value="other">Another link</option></select></label><label>Destination link<input name="destinationUrl" type="url" placeholder="https://..." required/><small>Paste the complete link, including https://</small></label>{error&&<p className="portal-message error">{error}</p>}<button className="portal-primary" disabled={loading}>{loading?<LoaderCircle className="spin"/>:<Check/>}{loading?"Activating…":"Activate product"}<ArrowRight/></button></form>}
